eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:7771/eureka/
server:
    port: 7771
spring:
    application:
        name: eureka-client
    cloud:
      config:
        label: master
        profile: dev
        discovery:
          enabled: true
          service-id: eureka-config
management:
  security:
    enabled: false
#        uri: http://localhost:8888/